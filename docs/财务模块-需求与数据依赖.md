# 世途旅行 · 财务模块产品需求文档（PRD）

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.2（公测阶段） |
| 更新日期 | 2026-06-11 |
| 文档状态 | **部分已确认**（6/11 贺帅团队回复 Q1/Q4/Q8/Q9/Q12/Q14/Q15，已批注于第 5 章；其余项待回复） |
| 适用读者 | 财务团队、管理层、产品/研发 |
| 系统位置 | 管理后台（:8080）→「财务」，仅管理员可见，访问留痕 |

---

## 1. 概述

### 1.1 文档目的

本文档说明财务模块**当前已实现的功能、数据来源与职责分工、核算口径**，并列出**待财务确认的事项**与**后续需求（二期）**，作为财务与产品之间的需求对齐依据。

请财务团队重点阅读**第 4 章（核算口径）**与**第 5 章（待确认事项）**，并在第 5 章表格中逐条回复。

### 1.2 重要前提 — 当前为演示数据

> **当前系统内的成本均为演示用估算值**（例如机票成本按售价的 70% 估算填入），**非真实采购成本**。
> 因此概览中的**毛利、航班贡献毛利目前仅用于展示报表格式，不具备业务参考意义**。
> 需待财务录入真实成本后，相关指标方可用于对账与经营决策。**在此之前请勿据此对账或汇报。**

---

## 2. 现状 · 已实现功能

### 2.1 功能总览

| 模块 | 用途 | 主要使用角色 |
|------|------|------|
| 概览 | 指定时间段的总收入 / 总成本 / 毛利 / 航班贡献毛利，收入按 10 项、成本按 16 项细分；可按航线圈定 | 财务、管理层 |
| 航班毛利 | 按航班班次列示：载客率、包机成本、单座成本、空座沉没、航班贡献毛利 | 财务、管理层 |
| 订单毛利 | 按订单列示：收入、成本、毛利率 | 财务 |
| 月度趋势 | 近 N 个月收入 / 成本 / 毛利走势；可按航线圈定 | 管理层 |
| 成本维护 | 录入成本：航班成本周期、单班次成本、产品成本 | 财务 |
| 报表导出 | 两类 Excel：① 按乘客明细 ② 按航班汇总 | 财务 |

### 2.2 成本录入入口

成本数据通过以下四处录入：

| 入口 | 位置 | 内容 |
|------|------|------|
| A. 航班成本周期（主用） | 财务 → 成本维护 → 航班成本周期 | 按「航班号 + 日期段」批量设定一组成本，区间内所有班次自动套用 |
| B. 单班次成本（例外覆盖） | 财务 → 成本维护 → 航班班次成本 | 个别班次与周期不同时单独录入，**单班次优先于周期** |
| C. 产品成本 | 后台「产品管理」录入，财务侧可查看/修改金额 | 酒店净房价、签证成本、接送结算价 |
| D. 订单杂项成本 | 订单详情 → 订单杂项成本 | 单笔订单的导游服务费、赠送费用、手续费、其他，可多条 |

**航班成本含 7 个科目**：包机总成本、机场税（去程）、机场税（回程）、燃油、旺季附加、机型调整、起降折扣（起降折扣可为负值，表示减项）。

> **示例（航班成本周期）**：`ZJ8888 / 2026-10-01 至 10-31 / 包机总成本 ¥80,000 / 机场税去 ¥50 / 机场税回 ¥50`。该航班 10 月内每个班次自动套用此组成本；若某日另有特殊价，在该班次单独覆盖即可。

### 2.3 报表与导出

- **按乘客导出**：一行一位乘客的成本利润明细（字段见附录 A）。
- **按航班导出**：一行一个航班班次的整班 P&L 汇总（字段见附录 A）；可按航线圈定（去回两个方向都导）。
- **经营报表**：销售毛利按 产品线 / 渠道 / 代理 / **航线** 四个维度，导出四张对应 sheet + 应收账龄 + 代理欠款。

> 机票行与套餐行的成本快照 2026-09-06 起落库，此前这两类行成本恒空、毛利一律显示「未知」。
> 口径、回填办法与航线维度见 **4.4**。

---

## 3. 数据架构 · 来源与职责分工

### 3.1 数据职责矩阵

> **核心原则：成本数据的完整性直接决定毛利核算的准确性。** 财务报表所用数据来自多个部门，职责划分如下：

| 数据项 | 存储位置 | 录入方 / 入口 | 财务权限 |
|--------|---------|--------------|---------|
| 订单收入 | 订单 | 前台散客/代理下单、后台批量创单 | 只读 |
| 已售座位数 | 航班班次 | 系统自动（下单即扣减） | 只读 |
| 乘客名单 | 订单 | 前台 / 代理录入 | 只读 |
| 酒店 / 签证 / 接送 成本 | 产品 | **操作部门（产品管理页）**（6/11 确认） | 只读 |
| 航班包机相关成本（包机、机场税、旺季附加、机型调整等） | 航班班次 / 成本周期表 | **财务（成本维护页，涉实付汇率；可单班覆盖）**（6/11 确认） | 可维护 |
| 订单杂项成本——导游服务费 / 赠送费用 / 手续费 | 订单 | **操作部门（订单详情）**（6/11 确认） | 可查看 |
| 订单杂项成本——"其他" | 订单 | **财务（订单详情，临时费用录入）**（6/11 确认） | 可维护 |
| 销售金额 | 订单 | **操作部门录入** → **出纳锁定** → **章琴审核到账**（6/11 确认） | 按环节 |
| 预期到账金额 + 锁定 | 订单 | **出纳（订单详情）** | 可维护 |

### 3.2 跨部门依赖与风险

- **财务可自主维护**：航班 7 项成本、订单杂项成本、预期到账锁定。此部分录入完整即准确。
- **依赖其他部门**：① 订单收入（前台/代理下单产生，正常下单即有）；② 产品成本（酒店/签证/接送，由产品管理维护）。
- **主要风险点**：若航班包机成本未录入、或产品成本未由产品部门维护，对应毛利将虚高，且系统不会报错——仅在概览的「缺成本条目数」处提示。因此需明确各项成本的录入责任人。

> 相关待确认：见第 5 章 **Q1**（职责划分）、**Q11**（缺成本提醒清单）。

---

## 4. 业务规则 · 核算口径

> 本章是财务最需核对的部分——以下口径若与贵司现行做法不一致，会直接导致毛利对不上。

### 4.1 收入与成本科目

**收入（10 项）**：去程机票、返程机票、去程机场税、返程机场税、酒店、签证、接送、导游、升舱及改期、超售。

**成本（16 项）**：去程包机、返程包机、去程机场税、返程机场税、旺季附加、燃油、机型调整、起降折扣（机场补贴）、房费、签证费、接送费、导游服务费、赠送费用、手续费（收款/汇款结算）、操作费（每单固定 ¥20，系统自动计提）、其他。

### 4.2 关键核算口径

| 指标 | 当前算法 | 说明 |
|------|---------|------|
| 机票成本 | 包机总成本 ÷ **总座位数** | 每位乘客分摊一个座位的成本；未售座位的成本计入「空座沉没」单列 |
| 单座（已售）成本 | 包机总成本 ÷ **已售座位数** | 保本参考线，用于定价；每多售一座即下降 |
| 去程 / 返程划分 | 同一往返订单按出发时间排序 | 最早一段为去程，其余为返程 |
| 机场税收入 | 暂按「代收代付、不赚不亏」 | 即机场税收入 = 机场税成本，假定向客户收取额等于支付额 |
| 航班贡献毛利（原"净利"，6/11 财务定名） | 毛利 − 空座沉没成本 | **注：未扣减公司期间费用（人力、租金、行政等），非公司最终净利润** |

### 4.3 核算示例（机票成本）

某航班包机总成本 ¥90,000，共 180 座，售出 150 座：

| 口径 | 计算 | 单位机票成本 | 空座沉没 |
|------|------|------------|---------|
| **(a) 当前系统**：÷ 总座位、空座单列 | 90,000 ÷ 180 | **¥500 / 人** | 30 × 500 = **¥15,000** |
| **(b) 备选**：÷ 已售座位、不单列空座 | 90,000 ÷ 150 | **¥600 / 人** | — |

两种口径的毛利结果差异显著，请于第 5 章 **Q5** 明确贵司采用哪一种。

### 4.4 成本快照与航线维度（2026-09-06 上线）

#### 快照 vs 实时：同一个「成本」在系统里有两套数

两套并存是刻意的，各有各的用途；页面上分别标了「快照 / 实时」，看数前先认准是哪一套。

| | 成本快照 | 实时成本 |
|---|---|---|
| 存在哪 | `OrderItem.totalCostCny`（下单那一刻写死） | 不落库，每次打开页面按当前成本周期重算 |
| 事后改成本周期 | **不追溯**，历史订单原地不动 | 跟着变 |
| 用在哪 | 经营报表全部维度、财务概览「区间内成本」KPI、月度趋势 | 财务概览的「成本细分（实时）」表、按航班导出、订单毛利页 |
| 为什么这样 | 月结要的是「当时这单花了多少」，账不能因为今天改了周期而变（Q12 已确认「不修改前期数据」） | 看的是「现在这条线的成本结构长什么样」，用来定价与谈判 |

两者对不上，通常就是成本周期在下单之后被调整过 —— 这不是 bug，按 Q12 的执行约定，
调周期应只动未来日期段。

#### 哪些行有快照

| 订单行 | 成本来源 | 上线时间 |
|---|---|---|
| 酒店 / 签证 / 用车 | 产品的成本价 × 数量 | 早已有 |
| **机票** | `resolveFlightItemCost`：（包机费 ÷ 总座位 + 机场税 + 燃油 + 旺季 + 机型调整 + 起降折扣）× 人数 | 2026-09-06 |
| **套餐** | 按套餐组件求和：住宿 晚数×每晚成本×房数 + 签证 办签人数×每人成本 + 用车 qty×每份成本 | 2026-09-06 |

三条口径上的要点：

1. **套餐行不含机票分量。** 套餐单里的机票是独立的机票行（挂着套餐 id），成本落在那些行上；
   套餐行只算地面。也**不用**「机票款 = 全包价 − 地面价」这类残差反推 —— 该拆法已在
   `docs/口径决议.md` 否决（只做展示报表、不驱动定价，更不该驱动成本）。
2. **算不出来就留空。** 班次与成本周期都没填、产品没录成本价、套餐绑的是随机档占位酒店 ——
   这些情况整行留 NULL，进「缺成本条目数」，页面照旧显示「未知」。绝不落 0：半个成本会让
   毛利虚高，而且是个精确又好看的假数字，没人会去质疑。
3. **快照跟着单子走。** 改期换班次 → 按新班次重打；换酒店 → 套餐行按住宿那一项的差额挪
   （算不出差额就转 NULL，不留旧店的数）；拆单 → 行成本按人搬，两侧相加与拆前守恒。

#### 存量回填

成本快照是 2026-09-06 才开始落库的，之前的单这两类行一律 NULL，存量区间的毛利仍报「未知」。
补法两条入口，跑的是同一份代码（`finances.cost-backfill.ts`），只填 NULL、可安全重跑：

- 命令行：`npx tsx scripts/backfill-item-cost-snapshots.ts`（缺省 dry-run 只算不写，
  加 `--apply` 才写库，`--limit=N` 试水）；
- 后台端点：`POST /finances/cost-snapshots/backfill?limit=&apply=true`（仅管理员）。

一条限制要先知道：成本周期没有版本历史，库里只有当前的周期定义。所以回填算出来的是
「按今天的周期 + 该航段出发日」得到的成本，不是下单那一刻周期长什么样；套餐的办签人数同理
取当前乘客名单。周期建好之后没改过的航线两者一致。新单不受影响 —— 新单在建单那一刻就把
快照落下来了。

#### 航线维度

第二条航线在即，报表原先只有 产品线 / 渠道 / 代理 三个维度，两条线的收入成本混在一个数里。

- **订单的航线**：本单最早起飞的那条航段的 `起飞地→降落地`（往返单即去程方向）；没有航段的
  纯地面套餐单退到套餐绑定的航班；两者都推不出来 → 归「未知航线」。**不兜底**到任何写死的
  航线 —— 兜底就意味着新航线的单静默算进老航线，错得毫无痕迹。
- 航线是**订单级**属性：一张单只归一条线，同单的酒店 / 签证行跟着订单走，不单独判。
- 落到页面：经营报表多一个「航线」页签（导出同步多一张 sheet）；财务页头部的航线下拉作用于
  概览、月度趋势、按航班导出三处。「未知航线」在下拉里是一个正式选项 —— 那批单正是需要去
  补绑航班的，藏起来「各条线加起来对不上总数」就永远查不出来。

---

---

## 5. 待确认事项（Open Items）

请财务团队逐条回复下表。回复后我们将据此调整系统口径与功能。

| 编号 | 待确认事项 | 财务回复 |
|------|-----------|---------|
| Q1 | 第 3.1 节「数据职责矩阵」是否与贵司实际分工一致？航班成本应由财务还是采购/操作录入？产品成本由谁维护？ | ✅ **已确认（6/11）**：包机相关成本（包机费、机场税、旺季附加、机型调整、机场补贴等）因涉及实付汇率，由**财务**维护；杂项中"其他费用"由**财务**维护（便于临时录入）；其余成本（房、签证、车、导服费、赠送费用、操作费、代理费等）由**操作部门**录入——操作是业务第一责任人和数据对接人 |
| Q2 | 航班 7 项成本是否齐全？是否还有其他实际发生的成本科目需补充？ | |
| Q3 | 成本「按航班号 + 日期段」设定的方式，是否符合贵司与航司的实际结算逻辑？（建议对照一张真实结算单核对） | |
| Q4 | 产品成本（酒店/签证/接送）应由产品部门录入、财务只读，还是财务也需可直接修改？ | ✅ **已确认（6/11）**：由**操作部门**录入维护（见 Q1） |
| Q5 | 机票成本口径采用 **(a) ÷ 总座位 + 空座单列** 还是 **(b) ÷ 已售座位**？（见 4.3 示例） | ✅ **已确认（6/15）**：采用 **(a) ÷ 总座位 + 空座单列**（"还是按现行的做法"）。即机票成本 = 包机总成本 ÷ 总座位、空座成本计入「空座沉没」单列、不计入航班贡献毛利——与现行系统口径一致，**无需改动**。`单座（已售）成本`保留为定价/保本参考列 |
| Q6 | 机场税向客户收取额是否等于支付额（不赚不亏）？若不等，需单独核算该项盈亏 | |
| Q7 | 收入 10 项 / 成本 15 项是否有冗余或缺失？（如是否需单列「超售成本」「退票损失」等） | |
| Q8 | 「净利」（未扣公司期间费用）是否更名为「航班贡献毛益」，以避免与公司净利润混淆？ | ✅ **已确认（6/11）**：更名为「**航班贡献毛利**」。系统界面已同步改名（6/11 上线） |
| Q9 | 出纳「预期到账 + 管理员锁定」机制是否符合需求？（当前：出纳填应收额，管理员锁定后他人不可改） | ✅ **已确认（6/11）**：销售金额链路为 **操作部门录入金额 → 出纳锁定金额 → 往来会计（章琴）审核到账数据**。与系统「预期到账 + 锁定」机制对应；审核用「按乘客导出」的结算到账列 |
| Q10 | 两份导出报表的字段是否够用？对照贵司现有对账模板，缺哪些列、列序是否需调整？（字段见附录 A） | |
| Q11 | 是否需要「尚未录入成本的航班/产品」提醒清单，便于财务掌握毛利完整度？ | |
| Q12 | 成本修改后是否影响已生成的历史订单？**(a) 不影响**（历史账锁定下单时成本，适合月结）/ **(b) 影响**（修改即重算全部相关历史毛利） | ✅ **已确认（6/11）**：**不修改前期数据**，避免数据混乱。⚠️ 执行约定：系统中修改"航班成本周期"会实时重算覆盖区间的历史毛利，因此请只对**未来日期段**调整成本，已过去的日期段不动（产品成本已有下单时快照，不受影响） |
| Q13 | 第 6 章二期需求的优先级排序？是否有遗漏的财务功能？ | |
| Q14 | **（6/11 新增）**回复中提到的"机场补贴"对应系统哪个科目？现有"起降折扣"可填负数作减项——是同一项，还是需单独新增科目？ | ✅ **已确认（6/11）**：**同一科目**。"机场补贴"即"起降折扣"（负数=补贴/减项），不另开科目。系统界面与导出表头已标注为"起降折扣（机场补贴）" |
| Q15 | **（6/11 新增）**回复中提到的"操作费、代理费"："操作费"是否即系统的"手续费"科目？"代理费"若指代理佣金，目前在结算模块单独核算，是否需并入成本口径？ | ✅ **已确认（6/12 更正）**："操作费"**不是**"手续费"。手续费=收款二维码结算公司手续费+国际清算行汇款手续费（合并为结算手续费）；操作费=订单录入及服务产生的人员服务费，**每单固定 ¥20**，已新增独立科目并由系统每单自动计提（可在订单详情改/删）。"代理费"仍=代理佣金，结算模块核算 |

---

## 6. 需求与路线图（二期）

以下为已收集、但**尚未实现**的需求。请在「优先级」列标注：🔴 高 / 🟡 中 / ⚪ 低。

| 编号 | 需求 | 提出方 | 当前状态 | 优先级 |
|------|------|-------|---------|-------|
| R1 | **银行流水对账**：导入银行流水，自动与订单应收匹配 | 章琴 | 未实现（现仅「预期到账 + 锁定」，无自动比对） | |
| R2 | **机场税收入独立核算**：当向客户收取额 ≠ 支付额时单独计算盈亏 | — | 未实现（现按不赚不亏处理） | |
| R3 | **升舱/改期/超售 收入录入入口**：科目已建，但前台/操作端尚无产生入口，目前只能作为订单杂项手工录入 | 贺帅 | 部分实现 | |
| R4 | **真实成本回填**：以真实成本替换当前演示估算值（属数据准备，非功能开发） | 全体 | 待执行 | |
| R5 | **退改/退款成本归集**：退票手续费、改期差价等成本的归集 | — | 未实现 | |
| R6 | **佣金与财务毛利打通**：代理佣金是否计入成本、如何并入概览 | — | 未实现（佣金现于结算模块，未并入财务毛利） | |

---

## 附录 A · 导出报表字段清单

**① 按乘客导出**（一行一位乘客）
订单号、乘客、出发日期、航班号、机票成本、机场税、旺季附加、燃油、机型调整、起降折扣、导游服务费、杂项（赠送+手续费+其他）、房费、接送费、签证成本、客单成本合计、客单收入、客单利润、各项收入、总成本、毛利。

**② 按航班导出**（一行一个航班班次）
航班号、路线、出发日期、总座位、已售、载客率、机票收入、其他收入、总收入、包机成本、机场税去、机场税回、燃油、旺季附加、机型调整、起降折扣、房费、签证费、接送费、杂项成本、总成本、整班毛利、单座（已售）成本、空座沉没。

---

*本文档随确认进度更新。回复第 5 章后，产品侧将据此迭代。* — 世途旅行

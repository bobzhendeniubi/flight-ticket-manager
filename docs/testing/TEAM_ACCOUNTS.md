# 测试团队账号 · Staging 环境

> 仅 staging 测试用，请勿在生产环境复用同样密码。

## 访问入口

| 入口 | URL | 说明 |
|------|-----|------|
| 客户/代理前台 | http://47.83.249.163/ | 客户下单、代理看团队/分成 |
| 后台管理 | http://47.83.249.163:8080/ | 管理员/运营用 |
| 后端 API（健康检查） | http://47.83.249.163/api/healthz | 仅排障 |

> 当前是 IP 直连，**未上 HTTPS**。后续切到正式域名 + Cloudflare 才会有 HTTPS。
> 测试期间不要在测试账号里填真实身份证 / 真实银行卡。

---

## 测试账号一览

**所有账号密码统一是：`Password123!`**（注意首字母大写、末尾感叹号）

| 角色 | 邮箱 | 在哪登录 | 能看到 / 能做什么 |
|------|------|----------|---------------|
| 管理员 | `admin@ftm.local` | 后台 :8080 | **全部权限**：订单批量改状态、审计日志、定价、结算单、代理管理 |
| 一级代理 | `agent1@ftm.local` | 前台 :80 | 自己 + 二/三级下级；自己 + 下级的分成 |
| 二级代理 | `agent2@ftm.local` | 前台 :80 | 自己 + 三级下级；自己 + 下级的分成 |
| 三级代理 | `agent3@ftm.local` | 前台 :80 | 仅自己；只能看自己的分成 |
| 客户 | `customer@ftm.local` | 前台 :80 | 我的订单、AI 助手下单 |

**注意事项**：

- 客户**不能在前台自助注册**（已禁用）。新客户由代理在后台开通账号。
- 代理之间是层级关系：一级看得到自己 + 二级 + 三级；二级看不到一级。
- 后台 :8080 只允许 ADMIN/STAFF 登录，代理或客户登入会被拒。

---

## 演示数据

种子已写入 6 张演示订单（订单号 DEMO-001 ~ DEMO-006），覆盖各种状态，方便测后台流转：

| 订单号 | 状态 | 用途 |
|--------|------|------|
| DEMO-001 | 已支付 | 测出票 / 进入处理 |
| DEMO-002 | 已出票 | 测完成 / 改期 / 退款流程 |
| DEMO-003 | 已完成 | 历史订单查询 |
| DEMO-004 | 退款申请中 | 测审批同意 / 驳回 |
| DEMO-005 | 已取消 | 终态展示 |
| DEMO-006 | 待支付 | 测取消 / 标记已支付 |

> 所有演示订单都归 `customer@ftm.local`。代理登入看不到（不是自己的客户）。
> 想测代理视角的订单，请用代理账号在前台 AI 助手 / 航班搜索手动下一单。

---

## 忘了密码 / 账号锁了怎么办

直接告诉 Bob 重置。staging 环境数据可以随时 reseed，不用慎重。

---

## i18n 切换

页面右上角下拉框可以切换 **中文 / English / Tiếng Việt**。
切换后会保存到浏览器 localStorage，下次访问自动用上次的语言。

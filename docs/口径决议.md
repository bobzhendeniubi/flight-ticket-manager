# 口径决议清单（单一事实来源）

> 规矩：每次业务口径拍板，**当场**在这里落一条（日期 + 决议 + 原因）。
> 新决议追加在最上面；被推翻的旧决议不删，标 ~~废止~~ 并注明被哪条取代。
> 本文件是口径的单一事实来源——微信聊天、commit message、AI 会话记忆都只是线索，以这里为准。

## 权限

- **2026-08-26 · 代理密码重置**：内部员工（ADMIN/STAFF）都可重置**代理**账号密码；重置内部账号（ADMIN/STAFF）仍仅 ADMIN——防员工借重置接管同事/管理员账号。重置后强制首登改密。
- **2026-08-25 · 航班维护岗**：航班/班次维护放开给「运营（STAFF 未设岗）+ 票务岗」：新建航班、新班次、批量加班次、批量改时刻、单班次改价/容量/时刻/上下架。仍仅 ADMIN：升舱差价/商务舱联动（定价敏感）、整线停售、批量删除班次、批量改容量、单班次删除（批量类爆炸半径大）。⚠️ 隐含约定：staffRole=null 即视为运营/通用岗。
- **2026-08-24 · 删单/回收站**：放开给 STAFF（没发管理员号）；强制改状态仍锁 ADMIN。
- **2026-08-24 · 账号**：一人一号、实名、按岗位配可见范围；共享号封存。

## 资金 / 返佣

- **2026-08-25 · 返佣计提**：按**起飞日 + 实收金额**计提；套餐独立成档；费率生效日可预配、按出发日取档、按北京时间显示。
- **2026-08-25 · 立减与佣金**（遗留待拍板）：佣金引擎按行计提**不含立减**——是否应含，未定。
- **2026-08-20 · 收款纠错**：撤销认款走专用闸（取消族放行）；手工收款可冲销；佣金有闸。
- **2026-07 · 调价原因白名单**：只许纯财务类（优惠/补收杂费/变更改期费/其它）；升舱走套餐加购、换酒店走换酒店功能、多签换签证产品。**手填价永不裸放开**——服务端权威定价是底线。

## 订单 / 库存

- **2026-09-05 · 随机档按城市圈定**：随机档 = **同 cityCode × 同 starRating** 的真酒店包房合计（城市事实源 = `Hotel.cityCode`，不从航班/机场反推）；聚合、缺口清单、超售闸、`RANDOM_TIER_SHORTFALL` 提醒全部按 (城市, 星级) 各算各的，A 城缺口不吃 B 城库存。占位酒店必须带 cityCode（守卫必填）；存量空城市回填 DAD。单独 HOTEL 行的「N 星随机」（行上无酒店）归存量默认城市 DAD——如要在第二个城市单卖随机行，需先给该行补城市字段（待办）。需求池/超售上限/占位判定口径不变。
- **2026-08-25 · 批量纠错**：批量改备注/改代理/改航班走 correction 语义；「改签」端点是售后语义（撤立减+推状态），纠错**不许**走改签。
- **2026-08-25 · 占位单**：三期方案见 docs/占位单-需求方案.md；守恒账本；建单弹窗必须有日期字段（0825 错留 55 座事故根因）。
- **2026-08 · 座位口径**：后台/代理单 paymentExpiresAt=null 永不自动退位；散客 30 分钟。SEAT_HOLDING/RELEASING_STATUSES 必须对称。
- **2026-07 · 软删**：占座先取消、净收款>0 禁删、回收站可恢复；2026-08-24 起删单放开 STAFF。

## 票务 / 开票

- **2026-07-08 · 开票模型**：三维布尔（去程/回程/系统 invoiced）；出票上限按航段计=班次座位容量；批量开票单次上限 50。
- **2026-07 · PNR 导出**：航司 27 列模版列序格式一格不能动；拆名支持斜线格式；PTC 按起飞日实足年龄。

## 定价 / 产品

- **2026-07 · 套餐定价**：整单 percent-off——运营填想卖的价→系统算折扣%，机票随出发日实时浮动。
- **2026-07-14 · 价格字段三层**：成本价（内部）/ 单卖展示价（前台「每晚起价」）/ 套餐权威价（下单）——价格类反馈先问清说的是哪层。
- **已否决 · 套票「机票款=残差」拆分**：业务确认不做；如再被提起，只做展示报表、绝不驱动定价。

## 品牌 / 文案

- 前台品牌=椰岛假期/Coco Holiday；世途/Citur 仅法律主体，前台不露出。
- 前台文案用买家视角（让利→已省，单房差→单人入住）；后台保留商家视角。
- 产品代码/UI/注释/测试名禁止出现内部同事姓名（详见仓库 CLAUDE.md 对照表）。

## 运维

- **2026-08-26 · 备份与监控**：服务器每日 03:30 快照保留 14 天 + 本机 launchd 每日拉取异地留存 30 天；看门狗每 5 分钟探双栈 /healthz，失败自愈重启+告警（渠道见 .watchdog.env）。
- **2026-08-24 · 双环境**：实测 /opt/ftm（main，按生产对待）+ 测试 /opt/ftm-staging（任意分支）；部署需老板逐次批准。
- 线上 PAYMENT_MODE=sandbox 且支付凭证全缺——改 live 会宕机；网关支付从未使用。

## 待拍板 · 金额口径冲突

> 2026-09-06 · 订单金额单一口径（`backend/src/lib/order-money.ts`，审查根因 R2）合并时**显形**的冲突。
> 每一条都是「同一个词、两个页面、两个数」，合并时**没有统一**——各自留了显式命名的函数（括号里），
> 黄金测试把两边的数都钉住了。拍板后改的是调用点选哪个函数，不是改公式。
> 差异用黄金夹具举例（F2 = 收 6300 + 预存抵扣 500、已完成退款 1000、应收 6300；F3 = 换人后应收 8750，
> 其中换人费 450 挂在被换人头上）。

| # | 同一个词 | 页面 A（算法） | 页面 B（算法） | 差在哪（夹具） | 建议默认 |
|---|---|---|---|---|---|
| 1 | **是否清账** | 三模板《全岗可用》/ 全岗总表：已付 + 预存抵扣 ≥ 应收，**不扣已完成退款**（`isSettledByPaidAmount`） | 财务导出「财务核对收入明细」：已收净额（扣已完成退款）≥ 应收（`isSettledByNetReceived`） | F2：A 显示「是」、B 显示「否」——先收后退的单在运营表上永远「已清账」 | 统一按 B（已收净额）：钱退回去了就不算清账；运营表两列跟着改 |
| 2 | **已收减已退**（REFUNDED 单的负项） | 财务概览 `revenueBreakdown.refund`：paidAmount − Σ已完成退款，**不含预存抵扣**（`paidMinusCompletedRefundsCny`） | 账龄 / 代理欠款 / 代理对账单 / 财务导出：paidAmount + prepaymentOffset − Σ已完成退款（`netReceivedCny`，lib/net-received） | F2 假设落 REFUNDED：A 5300、B 5800。**现状全库 prepaymentOffset 恒 0，线上数字今天相同**，公式不同 | 统一按 B（含抵扣）：抵扣也是客户付出的钱；将来「余额抵尾款」落到订单上时 A 会漏算 |
| 3 | **尾款 vs 应收余额** | 订单列表/详情 balanceDue、对账台候选、认款建议、提醒催尾款：应收 − 已付 − 抵扣，**不扣已完成退款**（`balanceDueCny` / `balanceDueCents` / `balanceDueDecimal`） | 应收账龄、代理欠款、代理对账单：应收 − 已收净额，**扣已完成退款**（`receivableBalanceCny`） | F2：A = −500（显示「多付」）、B = +500（显示「欠 500」）——同一张单列表说多付、账龄说欠款 | 退过款的单以 B 为准；A 那几处（列表 / 对账台 / 提醒）是否也扣已完成退款，请财务拍板（默认：扣） |
| 4 | **人均结算价** | 代理对账单「每人结算价」：应收 ÷ 人数，**换人费也摊**（`payablePerPaxCny`） | 三模板 / 全岗总表 / 分房表的均摊兜底：可摊应收 ÷ 人数，换人费 / 换人差价（excludeFromPerPax）**不摊**（`settlePerPaxFallbackCny`） | F3：A 2916.67、B 2766.67（按人表逐人是 2633.33 / 3033.33 / 2633.34） | 统一按 B：与每人份额算法（per-pax-share，换人费挂被换人）同源；代理对账单那列改调 |
| 5 | **营收 / GMV 含不含售后费** | 仪表盘营收、结算单 GMV、代理成交额：Σ `Order.total`，**不含** adjustmentCny（改期费 / 换人费 / 补房差）（`grossTotalCny`） | 代理对账单「应收」、列表金额列、账龄：total + adjustmentCny（`payableCny`） | F3：A 8100、B 8750。改期费 / 换人费今天**不进任何收入报表**（审查 D §2.7 同一条） | 售后费计入营收，报表单列「售后收入」；GMV 是否计佣基数另议（现状不计） |
| 6 | **三个「已付款」状态集合** | 仪表盘 `PAID_LIKE_STATUSES`：PAID / PROCESSING / TICKETED / COMPLETED / CHANGE_REQUESTED / CHANGED | 客户档案 & 结算单 GMV `PAID_STATUSES`：少 CHANGE_REQUESTED / CHANGED；代理成交额 `AGENT_STATS_PAID_STATUSES`：再少 PROCESSING | 改签中 / 处理中的单：仪表盘算营收、结算单不算 GMV、代理成交额也不算 | 统一为 `PAID_LIKE`（钱已收、单子还算数就是成交）；`lib/order-status-sets.test` 已把三者差异钉住，拍板后删两个 |
| 7 | **行程单应付的舍入** | 行程单 PDF `computeEffectivePayable`：`Math.round((n + Number.EPSILON) × 100) / 100`（'99.995' → 100.00，itinerary-pdf.test 钉死） | 其余全部：裸 `Math.round(n × 100) / 100`（→ 99.99）（`payableCny`） | 真实订单 total 是两位小数 Decimal、adjustmentCny 是整数，**永远得同一个数**；只有测试用的三位小数输入能分出来 | 无业务影响，二选一即可：建议行程单改调 `payableCny`，同步改那条单测 |

**不是冲突、只是备注：**
- 财务口径 `COUNTED_STATUSES` 含 REFUND_REQUESTED、库存口径 `INVENTORY_COUNTED_STATUSES` 不含——**这是既定设计**（申请退款那一刻座位已释放、钱还在账上），两者的差恰好只有它，测试有断言。
- 计佣基数（`computeCommissionBase`：可计提行毛额 × 净额/毛额，DISCOUNT 按比例摊、FEE 不进分母）只读侧已收成一个函数；`orders.service` 计佣落库那份内联算法**同式但仍是副本**（本批不碰写路径，等 R5 OrderMutation 内核落地再改调）。「立减是否入佣金基数」仍按上方「资金 / 返佣」2026-08-25 那条待拍板；现状立减快照行就是 DISCOUNT 行，已从基数里扣掉。
- 「已付≠水单」（收款行显示水单毛额 + 拆分去向）按上方待拍板处理，`paidCny` 原样封装 paidAmount，本批一分未动。

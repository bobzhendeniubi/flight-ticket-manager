# 跨单分房代码审查 B 路（泄露与前端）· codex gpt-6-astra · 2026-09-14

> 审查对象：分支 feat/shared-rooms@9e9f568（35 commit，四波合并后）。模型 gpt-6-astra，reasoning high，只读。提问见同目录 prompt 记录。

---

**总体判断：需修改，暂不通过本路审查。** 已核对 `main..HEAD` 的 35 个提交、需求 v2 和原评审。代理鉴权与字段脱敏已有实现，但仍有共享 ID 泄露、前端保存破坏共享状态、警告未展示及提醒生命周期遗漏。

以下行号均指当前 HEAD。未修改文件；Vitest 因缺少可用依赖未能启动，未运行数据库集成测试。已通过内存执行源码片段复现脱敏、编辑器保存、拖空共享房、提醒转换及编号反例。

**1. [HIGH] 代理仍能从房组 `id` 读出完整 `sharedRoomId`**

- **证据：**[hotel-control.shared-rooms.ts:652](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:652>) 将房组 ID 生成为 `shared:<sharedRoomId>:<orderItemId>`；[room-group-dto.ts:60](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-group-dto.ts:60>) 原样返回这个 ID。解散后的 `plain:` ID 也包含共享键。
- **为什么：**删除同名字段并没有删除值。实测 AGENT DTO 返回 `id:"shared:SHARED_SECRET:item-a"`。详情、列表及使用相同序列化器的售后订单响应都受影响；解绑后也可能保留这个泄露。
- **修法：**本单房组使用独立、稳定、不编码共享关系的 ID，并迁移历史派生 ID。脱敏测试须使用真实写入形状，断言整个响应不包含共享键值，而非仅检查不存在某属性。

**2. [HIGH] 把已保存共享房拖空后，保存会遗漏旧房，可能留下双重共享成员**

- **证据：**[SharedRoomWorkbench.tsx:409](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/SharedRoomWorkbench.tsx:409>) 过滤空房，版本也只收集剩余房；[hotel-control.shared-rooms.ts:467](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:467>) 仅从请求房间和 `dissolve` 确定旧共享房。
- **为什么：**将 S 的全部乘客拖入新房 T 后，实测 payload 只有 T，`expectedVersions={}`、`dissolve=[]`。服务端会创建 T，却不删除 S 的成员；订单 JSON 又会移走 S 的乘客，造成成员表与 JSON 分叉。若全部拖回池，则提示“还没有任何改动”，原共享关系仍在。
- **修法：**显式表达源房清空/成员迁移，并提交源房版本；后端根据乘客现有成员关系补齐受影响旧房。不能将“省略房间”当作删除，也不能混淆“解散后退回普通组”和“迁入另一共享房”。

**3. [HIGH] 单单保存虽然接受共享组零份额，却把行级 `roomsBilled=0` 清成 `null`**

- **证据：**[orders.routes.ts:1584](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1584>) 先清空酒店行计费房数；[orders.routes.ts:1602](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1602>) 跳过所有 `rooms<=0` 的回写。
- **为什么：**提交完全未改动的零份额共享组、仅修改备注，也会把显式零变成 `null`。这违反 v2 的“零必须显式落库”，并重新激活下游兜底语义。共享成员关系没被删除，但保存并非只改备注。
- **修法：**按 reconcile 后的房组计算回写，零也必须写入；补充“共享零份额仅改备注”的持久化断言。

**4. [MEDIUM] DTO 与编辑器协议不一致：代理锁定失效，内部用户只改备注也无法保存**

- **证据：**[RoomingEditor.tsx:198](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/RoomingEditor.tsx:198>) 只认 `sharedRoomId`，忽略外部 DTO 的 `isShared`；[RoomingEditor.tsx:442](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/RoomingEditor.tsx:442>) 保存时重新派生酒店名；[orders.routes.ts:1493](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1493>) 要求酒店名与原值严格相同。
- **为什么：**
  - AGENT 收到 `isShared:true` 后，编辑器却判定未锁定，零份额变成 1、归属丢失，拖拽/下拉/删除入口重新开放；最终由后端拒绝，**不是成功越权**。
  - 内部用户的共享组原始酒店名为 `''`，保存时变成真实酒店名，导致只改备注也触发 400。
- **修法：**类型和锁定逻辑支持 `isShared`；共享组采用真正的备注补丁协议，或完整保留不可变字段。不要对锁定组重建酒店名、裁剪房型或重新推断归属。

**5. [MEDIUM] 解绑 warnings 在批量响应和单笔 UI 中丢失**

- **证据：**[orders.service.ts:7719](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:7719>) 将警告保留在 `audit`；[orders.routes.ts:1909](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1909>) 返回时删除整个 `audit`。单笔 API 类型仍只有 `order`，例如 [api.ts:5672](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/lib/api.ts:5672>)；[HotelSwapModal.tsx:275](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/HotelSwapModal.tsx:275>) 成功后直接关闭/回调。
- **为什么：**批量改班次完成解绑，响应却无逐单警告；换酒店、酒店改期及纠错页面也不展示后端新增 warnings。“自动解绑并明示”只完成了服务端的一部分。
- **修法：**批量结果显式返回逐单 warnings，审计同步保留；更新各售后 API 返回类型与成功提示，在关闭弹窗前展示警告。

**6. [MEDIUM] 一间共享房含取消成员，会阻断工作台保存其他房间**

- **证据：**[hotel-control.shared-rooms.ts:144](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:144>) 的订单池仅含有效单，但共享房返回全部成员；[SharedRoomWorkbench.tsx:94](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/SharedRoomWorkbench.tsx:94>) 将这些成员全部放入草稿；保存会提交所有非空房。后端在 [hotel-control.shared-rooms.ts:511](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:511>) 拒绝无效状态订单。
- **为什么：**取消成员没有姓名 chip 可操作，却仍在提交数据中；即使只新增另一间房，也会被这张取消单挡住。
- **修法：**只提交实际变更房间；明确展示历史成员及状态，并提供符合“取消保留成员”语义的处理入口。

**7. [MEDIUM] 工作台重存会清除本单房组备注**

- **证据：**[hotel-control.shared-rooms.ts:584](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:584>) 丢弃触及房间的旧房组；[同文件:651](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:651>) 重建时没有保留 `notes`。
- **为什么：**单单编辑器允许维护本单备注，但下次工作台保存同一房间就会将其清掉；工作台目前还提交全部房间，扩大了影响范围。
- **修法：**按共享房、订单及住宿行匹配保留本地备注；继续与内部 `SharedRoom.notes` 分离。此外编辑器清空备注应显式提交空串，否则当前“省略空备注 + 服务端保留旧值”无法清除备注。

**8. [MEDIUM] 提醒仅在单轮候选中互斥，落库状态没有完整同步**

- **证据：**[reminders.rules.ts:1217](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/reminders/reminders.rules.ts:1217>) 仅在所有乘客已覆盖时关闭 PARTIAL；[同文件:1237](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/reminders/reminders.rules.ts:1237>) 将任何已存在的 ruleKey 都视作不可再次生成。
- **为什么：**
  - 已有 PARTIAL 后清空全部分房，会新增“整单未分房”，旧 PARTIAL 不关闭，实测关闭键为空。
  - 全部分好自动关闭后再次移出乘客，同日期 PARTIAL 不会重新打开。
  - 改入住日期后仅处理新日期键，旧日期提醒也可能残留。
- **修法：**按订单同步当前分房提醒状态，处理两种规则互相替代、日期变化和条件复发；区分人工核销与自动消解。

**9. [MEDIUM] 下钻三列与“共享房主单已取消”提示未接入 UI**

- **证据：**后端已返回新增三列及说明，但 [HotelControlPage.tsx:537](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/pages/HotelControlPage.tsx:537>) 只读取 `occupants`，[同文件:610](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/pages/HotelControlPage.tsx:610>) 仍显示一个 `rooms`；[同文件:1500](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/pages/HotelControlPage.tsx:1500>) 的提醒计数也未纳入 `sharedRoomOrphaned`。
- **为什么：**用户仍看不到参与共享房数、计费份额及不可相加的说明；后端已有主单取消告警时，页面仍可能显示“暂无提醒”。
- **修法：**补齐 API 类型、三列展示、`detailNote` 和孤立共享房告警；保存后刷新相关下钻与提示。

**10. [MEDIUM] 统一编号器仍不能保证三个导出给同一共享房相同房号**

- **证据：**[room-identity.ts:67](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:67>) 按首次遇见顺序编号；[orders.export-master.ts:840](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export-master.ts:840>) 按创建时间降序，而 [orders.export.ts:483](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export.ts:483>) 按升序。分房表还会给未分房乘客占用编号，总表则显示“未分房”。
- **为什么：**同酒店同入住日存在 S1、S2 时，两种遍历顺序即可把房1、房2对调。共享同一算法保证了**单份导出内**同房同号，没有保证跨导出编号一致。
- **修法：**三个入口使用同一份预先构建的身份→房号映射，统一排序、编号集合及未分房处理；用至少两间共享房加未分房乘客做跨导出断言。

**11. [MEDIUM] 导出缺少“合住方已取消”状态提示**

- **证据：**[room-identity.ts:101](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:101>) 只读取伙伴单号，不读取状态或软删除标志；[同文件:124](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:124>) 只能生成普通合住文案。
- **为什么：**取消/软删成员虽被查询保留，却无法标记其失效；代理也只能看到“与他单合住”，缺少 v2 要求的取消提示。
- **修法：**lookup 返回伙伴状态，内部与代理分别格式化；代理版本继续不带单号。

**代理出口逐项核验**

| 出口 | 判断与证据 |
|---|---|
| 订单详情 | **HIGH：泄露共享 ID 值**，见 finding 1。接入点 [orders.service.ts:5253](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:5253>)。未发现新增共享伙伴乘客联查回包。 |
| 订单列表 | **HIGH：同上**，接入点 [orders.service.ts:5113](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:5113>)。 |
| 售后响应 warnings | **INFO：角色分流正确**。换酒店、酒店改期、机票平移均按 actor.role 取文案；[shared-room-unbind.ts:240](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/shared-room-unbind.ts:240>) 的代理文案没有共享键、伙伴单号或乘客。响应中的 `order` 仍受 finding 1 影响。 |
| 代理可下载模板 | **INFO：新增合住备注未泄露伙伴信息**。[orders.export-templates.ts:1032](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export-templates.ts:1032>) 使用中性文案；订单范围由路由强制限定。 |
| 全岗总表 | **INFO：代理视角被服务端强制指定**，[orders.routes.ts:944](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:944>)；合住备注走中性分支。虽然仍查询伙伴 lookup，但没有因此输出伙伴信息。 |
| 审计 | **INFO：AGENT 403**，[audit.routes.ts:12](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/audit/audit.routes.ts:12>)。不能算代理开放泄露口。 |
| 提醒 | **INFO：AGENT 403**，[reminders.routes.ts:33](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/reminders/reminders.routes.ts:33>)；订单序列化也清空 reminders。 |
| 房控两个新端点 | **INFO：均受 ADMIN/STAFF 鉴权保护，AGENT 403**，[hotel-control.routes.ts:348](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.routes.ts:348>)、353。 |
| 批量改班次 | **INFO：AGENT 403**，[orders.routes.ts:1825](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1825>)。内部使用者收不到解绑 warnings，见 finding 5。 |

`REDACTED_ITEM_METADATA_KEYS` 已覆盖 `splitPairKey`、`splitFromItemId`、`sharedRoomId`（[orders.service.ts:27077](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:27077>)）。**未发现本次改动直接返回 splitPairKey；但 metadata 黑名单无法解决房组 ID 编码泄露。**

**单单 PUT、前端校验与提醒窗口的补充结论**

- **INFO／权限通过：**客户端 `sharedRoomId`、`splitPairKey` 被 Zod 剥离；旧共享组按锁后状态保留，改乘客、归属、酒店、房型、份额或删除整组均拒绝。普通组传 `roomFraction=0` 返回 400。不能据此伪造或换掉共享关系；零份额持久化另有 finding 3。
- **INFO／正常校验通过：**工作台默认首个成员组 1、后续 0，按订单行分组求 Σ份额；当前操作只能产生 0.5 步进，与后端一致。正常版本冲突会显示服务端提示和刷新入口。旧单全部房组需补归属的引导与后端一致。
- **INFO／内部锁定通过：**持有 `sharedRoomId` 时，拖入、拖出、归属修改、份额修改及删除都有函数层保护；代理 DTO 路径失效见 finding 4。
- **INFO／窗口正确但不能保证首轮量小：**部分未分房沿用最早入住日 **今天至未来 3 天**，今天/明天 CRITICAL，其余 HIGH；排除 `documentNumber='N/A'`。不会给全部历史订单新建提醒，但**窗口内所有存量部分未分房单都会在首轮补建**，没有上线日期过滤或批量上限。实际数量需查库，不能从代码断言不会大量出现。

**文案与 notes 核验**

新增行未命中 `CLAUDE.md` 所列内部姓名或“按某人口径”等署名文字。新增共享伙伴单号在内部导出时动态生成，未发现自动写入本单 `notes`；`SharedRoom.notes` 也与对外备注分离。

但“仓库没有把内部单号写入代理可见 notes”这个结论**不成立**：既有拆单路径在 [orders.service.ts:18535](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:18535>) 写入“由订单 … 拆分创建”，代理可见 `order.notes`，模板与总表也拼入它。**[MEDIUM，存量问题]** 这不是本分支新增的共享伙伴泄露，但应迁入内部溯源字段。此外，自由备注原文仍对外透传，不能将“自动合住文案安全”扩大为“任意备注内容均已脱敏”。

**原 finding 8／9／10／11 核实**

| 原 finding | 当前结论 |
|---|---|
| **8：代理泄露** | **部分修复，HIGH 未关闭。** 角色 DTO、metadata 黑名单、代理导出文案和 warnings 分流已接入；共享键仍藏在房组 ID 中。审计、提醒、房控及批量端点继续不对代理开放。 |
| **9：编辑器丢键** | **部分修复，未关闭。** 同 ID 重存的 splitPairKey 由服务端保留；内部共享锁覆盖了拖拽等路径。但代理 `isShared` 未接、共享组保存改酒店名、零份额回写及本地备注仍有问题。 |
| **10：导出** | **部分修复，MEDIUM 未关闭。** 共享身份、酒店 ID 作用域和同份导出同房同号已实现；跨导出编号及合住方取消提示仍未完成。 |
| **11：下钻与提醒** | **部分修复，MEDIUM 未关闭。** 后端新增三列与 PARTIAL 规则，但 UI 未消费完整信息，提醒互斥、复发及旧日期清理仍不完整。 |
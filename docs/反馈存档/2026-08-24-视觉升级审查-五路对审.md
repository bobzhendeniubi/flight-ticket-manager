# 视觉/UI 升级审查 — 五路对审合并报告（2026-08-24）

审查方式：4 路 Claude Sonnet 分面审查（仪表盘可视化 / 后台工作流 / 前台商城 / 设计系统横切）+ codex（gpt-5.6-sol @xhigh）全局审查，全部只读。触发背景：仪表盘时区 bug 排查 + 航班管理页截图暴露的按钮/emoji 问题。

**≥3 路共同点名的问题标 ★（可信度最高，优先做）。**

---

## T0 · 诚信/品牌类（不是"变好看"，是"不能这样上线"）

1. **信任承诺与事实不符**（codex 独家）：前台共享组件固定宣称"安全支付、出发前 7 天可退、7×24 中文客服、正规持牌经营"，但实际是线下核款、退改有 ¥500/¥800 扣损、别处又写 7×12 客服。`sales-web/src/components/TrustBadges.tsx:17`、`RefundBadge.tsx:12`、`lib/notices.ts:25`。→ 改为按产品/取消政策动态生成的真实承诺，未核验的资质不展示。M（3–5 人日）
2. **联系页 placeholder 直接示人 + 假留言成功态**（codex 独家）：电话/邮箱/牌照/ICP 显示"待补"，留言表单不发请求却渲染"已收到留言"。`sales-web/src/pages/ContactPage.tsx:24,44,118`、`AboutPage.tsx:49`。→ 未就绪模块隐藏；表单要么接真渠道要么改成电话/微信跳转。S–M
3. **小程序导航栏仍叫"世途旅行"、配色还是旧后台深蓝**（codex 独家）：`miniprogram/src/app.config.ts:16`、`app.scss:3`。小程序目前未上线，但上线前必须改成椰岛品牌。M
4. **域名品牌露出（两模型有分歧，待拍板）**：canonical/OG/robots/sitemap 都是 `store.citurtravel.com`。codex 认为搜索结果/分享卡片会露出法律主体品牌，建议迁移 Coco Holiday 域名（L，需域名/部署协同）；Sonnet 前台审查员认为按既有约定"域名可保留、品牌名不露出"不算违规。→ 运营拍板。
5. **报表"占比"口径误导**：`ReportsPage.tsx:265-268,351-355` 实际算的是"相对最大值"，表头却写"毛利占比"，财务读数会误判权重。代理欠款表同款。→ 改真实份额或改列名，与财务对口径。0.5 天

## T1 · 高价值低成本（1 天内/条，先做这批）

6. ★ **状态徽章色彩三套各自为政**（4/5 路点名）：同一 `PAID` 在仪表盘 `badge-info`、实时订单 `badge-success`；`DashboardPage.tsx:23-32` 漏 4 个状态静默灰化；`OrdersPage.tsx:63-77`、`HoldOrdersPage.tsx:38-44`、`RealtimeActivity.tsx:15-29` 互不引用；另有 10+ 处页面自拼色表绕开 `.badge-*`（`AgentsPage`、`SettlementsPage:315`、`ProductsPage:1857` 等）。→ 抽唯一 `lib/orderStatus.ts` + `StatusBadge` 注册表，按"进行中/成功终态/待关注/失败终态"语义分组，颜色外加图标区分（色盲可辨）。1–2 天
7. ★ **涨跌/警示色语义自相矛盾**：`DashboardPage.tsx:126-128` 跌用 rose、同页 `:259-260` KpiCard 跌用 amber；报表/财务/账龄卡又各有一套色阶（`SeatStatsPage:344-350`、`PendingAgingCard:58-63`、`FinancesPage:268-276` 三套相似阈值色）。→ 补 `--trend-up/--trend-down` token + 通用 `getSeverityColor()`。0.5 天
8. ★ **危险操作无危险色**（截图坐实 + 2 路核查）：`.btn-danger` 已存在但全仓只有 3 个文件在用。`FlightsPage.tsx:180-189` 批量删除班次=普通按钮；`:535-539` 裸 button 连 focus 环都没有；`:3042` 用内联类硬改 `btn-primary` 伪装危险色。→ 全仓 grep 删除/作废/撤销按钮统一 `.btn-danger`，约 20–30 处。1 天
9. **emoji 当图标约 35 处**（截图坐实 + 2 路 + admin-web 无任何 Icon 组件）：`FlightsPage` 🧳🗑📅、`OrdersPage` 🗑、`DashboardPage` 🥇🥈🥉、`ProductsPage` 等；sales-web 的 `CheckoutPage`/`AiAssistant` 各 8 种。→ 参照 `sales-web/src/components/Icon.tsx` 给 admin-web 建 10–15 个线框 SVG 图标逐步替换；奖牌先换数字角标。1–2 天
10. **`ink-muted` 对比度硬伤**（2 路独立测算一致）：admin 2.56:1 / sales 2.90:1，远低于 AA 4.5:1，且是全站最高频小字色；codex 补充前台 `deal` 白字 3.10:1、`brand` 白字 4.07:1 也不达标（CTA 按钮）。→ 加深 token 值 + 半天视觉走查。小
11. **机票搜索页整页没套椰岛设计系统**（前台路独家、性价比最高）：`HomePage.tsx` 全篇裸 slate 色、价格用裸红不用 `.price` 珊瑚色、手搓 tab 不走组件类——像重设计前旧版漏迁移。→ 纯样式替换。0.5–1 天
12. **首页品类预览图没走 `Img` 组件**：`HomeSections.tsx:128,225` 裸 img 拉 Unsplash 原图，移动端也拉桌面大图（LCP/带宽）。→ 换 `<Img ratio="4/3">`。0.5 天
13. **财务/结算 6 个文件的表格绕开 `.table-admin`**（2 路点名）：`FinancesPage`（全文 0 处）、`HotelControlPage`、`SettlementRatesPage` 等——最需要 `tabular-nums` 对齐的数字大表反而没用。→ 批量补类。1 天
14. **航班卡操作层级重排**（截图触发，3 路方案一致）：主操作只留"查看班次/新班次"；行李/批量归二级工具条；停售/删除进危险菜单；"（已联动）"状态从按钮文案改成 badge，解决按钮列不齐。M（可先做纯样式版 1 天）

## T2 · 结构性升级（立项做）

15. ★ **仪表盘从数字陈列→驾驶舱**（3 路）：手写 div 柱状图无 tooltip/坐标/参照线，订单量维度拿了数据没画；"最近订单/最新订单"两模块重复；账龄卡 4 个孤立数字看不出分布。→ 手写 SVG 组合图（双轴+自定义 tooltip+日均参照线，不引库守 bundle 预算）、账龄加水平堆叠条、合并重复模块换成"待处理/临期/超售/待认款"任务入口。1.5–3 天
16. ★ **订单详情"假抽屉"→真 Side Sheet**（2 路）：`OrdersPage.tsx:3060-3099` 名为 Drawer 实为 672px 居中弹窗，塞十余区块，无 `role="dialog"`/焦点陷阱。→ 右侧 720–900px sheet + 锚点分区 + 可访问 Dialog。2–3 天
17. ★ **Modal 无障碍批量迁移**（3 路）：admin-web 23 处 `fixed inset-0` 裸弹层只有 2 处带 dialog 语义，全仓 0 焦点陷阱；而 `sales-web/src/components/Modal.tsx` 已是规范实现（焦点陷阱/Esc/焦点归还/reduced-motion）。→ 原样移植到 admin-web 逐步替换；顺带替换 44 处 `window.confirm` + 81 处 `alert`（高风险操作先行）。3–5 天分批
18. **订单筛选区重构**（2 路）：十余项五列平铺 + "勾选优先"隐藏规则靠小字解释。→ 基础筛选 + 高级抽屉 + 可点掉的条件 chips + 岗位保存视图（票务待开/今日出发/待认款）。行内"改状态…"空值下拉换成详情里已有的 `allowedNext` 标准流转按钮模式（`OrdersPage.tsx:3300-3360` 现成逻辑搬回列表）。2–3 天起
19. **套餐列表卡瘦身**（2 路）：单卡塞日期/双航段/房型/8 行拆价/升级 stepper/三按钮，是"配置器"不是"商品卡"。→ 列表专注比选（图+价值主张+含什么+总价+单 CTA），配置进详情页。3–5 天，单独立项
20. **结算页假三步→真渐进**（2 路）：`CheckoutPage.tsx:1007-1028` 步骤条纯装饰；不可用的优惠码输入框在展示。→ 真锚点分步 + 校验进度；优惠码上线前隐藏。3–5 天
21. **收款对账双栏工作台**（codex）：认领在十列表格里插行展开，行高跳变。→ 左 inbox 右详情。2–3 天
22. **财务页决策型图表**（codex）：全表格无图；月度趋势红条盖绿条。→ 瀑布图+分组柱+毛利率折线+异常点下钻。2–4 天
23. **座位统计加"航班×日期"热力图**（codex）：识别连续低载/超售区间。2–3 天
24. **骨架屏/空态/错误态统一**（3 路）：全站"加载中…"纯文字，加载态与空态难区分，前台首页区块失败静默消失（`HomeSections.tsx:201,261`）。→ `TableSkeleton`/`CardSkeleton`/`EmptyState` + 错误重试 + `aria-live`。1–2 天

## T3 · 低优先级

25. 目的地灵感区加真实摄影+Fraunces 展示字（0.5 天）；详情页点缀 `wave-divider`/`grain` 承接氛围（0.5 天）；Hero 内加主 CTA 承接交易（M）
26. KPI 卡主次分级 + 大数防溢出缩写（半天）；Top 代理奖牌 emoji 换数字角标（2h）
27. 侧栏 ⌘K 快捷搜索/收藏/最近访问（M）；顶栏加分组面包屑（0.5 天）
28. `darkMode: 'class'` 死配置：删掉或真做（若做优先 admin-web，前提先收敛组件漂移）
29. 字体 preload 补正文字重（1–2 个标签）；"单房差"加人话解释 tooltip（<1h）；RealtimeActivity 新订单进场过渡（1–2h）
30. 报表页加"数据截至"时间戳（1h）

## 值得肯定（两路独立确认）

- 颜色 token 纪律好：全仓 .tsx 几乎无裸十六进制色，问题是"选错 token"不是"绕开 token"
- `sales-web` 的 `Modal.tsx`（无障碍范例）、`Img.tsx`（类型层强制 alt）、`RealtimeActivity`（实时感范例）都是可直接推广的现成资产
- 前台无"世途/Citur"品牌名露出（域名除外，见 T0-4）；余位档位化、评价不造假等纪律已落实

## 建议落地顺序

1. **第一批（约 3–4 天）**：T0-1/2/5（诚信类）+ T1-6/7/8/10（状态色系统+危险色+对比度）——全是低风险高收益
2. **第二批（约 1 周）**：T1 剩余（emoji 图标/HomePage 迁移/表格类/航班卡）+ T2-15 仪表盘
3. **第三批（立项）**：订单 Side Sheet、Modal 迁移、筛选重构、套餐卡瘦身、结算真分步
4. T0-3 小程序品牌改造放到小程序上线前；T0-4 域名迁移等运营拍板

（本报告为内部资料；所有产品侧改动执行时须遵守"产品内禁止内部人名"铁律。另：仪表盘 UTC 分桶时区 bug 与导出出发日期差一天问题为功能性 bug，不在本报告范围，见当日会话记录。）

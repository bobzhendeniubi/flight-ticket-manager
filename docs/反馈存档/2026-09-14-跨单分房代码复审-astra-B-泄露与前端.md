# 跨单分房代码复审 B 路（泄露与前端）· codex gpt-6-astra · 2026-09-14 晚

> 复审对象：feat/shared-rooms@c48c867。模型 gpt-6-astra，reasoning high，只读。

---

# 总体判断：需先修，当前不建议上线

复审范围：`feat/shared-rooms@c48c867`，对照 `eb278b7..HEAD` 指定 diff，并追踪必要的序列化、拆房组、解绑及调用链。

**必修项：**

- 补齐存量共享键脱敏：现有迁移遗漏订单行 metadata 中的旧编码 ID。
- 修正成员迁移：显式解散产生重复归属，隐式迁出丢失留守方 JSON。
- 修正代理保存协议、普通零份额回传及失效成员保留语义。
- 修正导出的日期作用域、拆单身份排序和配对核验。

**验证边界：**未修改文件。Vitest 因依赖不存在未能启动，未运行数据库集成测试或浏览器交互测试。已在内存执行源码函数／计算片段，复现脱敏遗漏、零份额变化、两类迁移错误及三类编号反例；提醒状态转换也做了函数级验证。

# 一、B 路 11 条逐项核实

下表行号均为当前 HEAD。

| Finding | 结论 | 核实结果与证据 |
|---|---|---|
| **B1 房组 ID 泄露** | **部分** | 新建及解散组已改独立 UUID，更新保留非旧编码 ID；`plain:` 新写入也已去掉。见 [hotel-control.shared-rooms.ts:925](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:925>)、`:966`。迁移识别 `shared:`／`plain:`，见 [rewrite-shared-room-group-ids.ts:65](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/scripts/rewrite-shared-room-group-ids.ts:65>)。但 DTO 仍原样返回 `id`，未迁移数据仍泄露；**迁移后 metadata 的旧 ID 也可能继续泄露**，见新发现 N1。不能判定“整个响应再无共享键值”。 |
| **B2 拖空旧房及隐式旧房** | **修坏了** | 前端空旧房已进入 `dissolve`，并提交源房版本，见 [SharedRoomWorkbench.tsx:533](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/SharedRoomWorkbench.tsx:533>)、`:559`。后端已按请求乘客查找同酒店、同区间旧房，见 [hotel-control.shared-rooms.ts:623](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:623>)。但显式解散后迁入新房会重复归属；隐式旧房有留守成员时会丢镜像，见 N2、N3。 |
| **B3 单单保存零份额** | **已解决** | 共享组的 `0` 经 reconcile 保留，再按最终房组回写；引用过但不再承载份额的行也显式写 `0`，不再先清成 `null`。见 [orders.routes.ts:1496](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.routes.ts:1496>)、`:1609`、`:1644`。**普通零份额的前端路径仍有另一处问题**，见 N4。 |
| **B4 锁定、原样回传、空备注** | **部分** | `isShared` 已参与锁定；内部共享组保留原酒店名、房型和归属；空备注显式回传，后端空串清除、省略保留。见 [RoomingEditor.tsx:208](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/RoomingEditor.tsx:208>)、`:250`、`:458`；`orders.routes.ts:1498`。**代理仍无法正常保存共享组备注**：外部 DTO 不含 `orderItemId`（[room-group-dto.ts:59](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-group-dto.ts:59>)），前端因此省略它，后端却在 `orders.routes.ts:1486` 严格比较旧归属，正常请求仍然 400。 |
| **B5 warnings 返回及展示** | **部分** | 换酒店、酒店改期、纠错的返回和展示已接通；批量正常成功分支也保留逐单 warnings。见 `orders.routes.ts:2420、2855、2892、1957`，[OrdersPage.tsx:2498](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/pages/OrdersPage.tsx:2498>)、`:8533`、`:8604`、`:8639`。遗漏两路：**普通按人／全员改期**的警告留在 `audit.reschedule.warnings`，前端 `OrdersPage.tsx:9271` 只调用 `onSaved(res.order)`；**批量“已生效、回包异常”**分支仍返回 `warnings: []`，见 `orders.service.ts:7853`。 |
| **B6 失效成员** | **部分** | 未变更房间不再一并提交，灰色只读 chip 已有；后端按原成员集合及份额判断“未变更放行”，见 `SharedRoomWorkbench.tsx:531、868`、[hotel-control.shared-rooms.ts:728](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:728>)、`:769`。但前端提交变更房间时自动剔除失效成员，与后端“提交集合就是最终集合”冲突，见 N5。后端补的姓名／单号快照也尚未被灰色 chip 消费，仍只显示状态。 |
| **B7 本单备注保留** | **已解决** | 更新同一共享房及解散退普通组时，按共享房、订单、住宿行保留本地 notes；与 `SharedRoom.notes` 分离。见 [hotel-control.shared-rooms.ts:877](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:877>)、`:933`、`:975`。单单清空备注语义也已修正。此结论针对**同房重存／解散**，不代表迁入新房会继承旧房备注。 |
| **B8 提醒状态机** | **部分** | 启用范围内，互斥、自动消解后复发、旧日期清理均已实现并通过函数级反例。人工核销通常不重开。见 [reminders.rules.ts:449](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/reminders/reminders.rules.ts:449>)、`:472`。但启用日期只限制重开／清理，**不限制新提醒创建**：`:628` 生成候选，`:1412` 全量创建。默认日期为 `2026-09-15`（`:348`），不能据此保证首轮量小；启用日前当前状态仍有旧日期残留。取消／软删订单又被 `:1002` 排除，无法靠本状态机清理其旧提醒。 |
| **B9 下钻三列及孤立共享房 UI** | **部分** | 三列、说明、`sharedRoomOrphaned` 计数及文案已接入，见 [HotelControlPage.tsx:605](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/pages/HotelControlPage.tsx:605>)、`:623`、`:1518`、`:1592`。但工作台保存仅更新 `boardNonce`（`:488`），打开中的下钻不重新取数；提醒组件只依赖 token（`:1515`），也不会随保存刷新。 |
| **B10 跨导出编号** | **修坏了** | 已增加预编号，未分房改为在已分房号之后续号，原“未分房先占号挤动共享号”得到缓解，见 [orders.export-room-allocation.ts:740](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export-room-allocation.ts:740>)。但预编号合并日期、拆单身份仍取首次遇见的排序键，且配对核验错误合并同 token 的不同拆单关系，见 N6–N8。未分房在总表仍显示“未分房”，另两表继续打包编号；三表没有统一其编号表达。 |
| **B11 合住方取消标注** | **部分** | lookup 已取状态／软删信息，内部文案带“已取消”，代理保持中性且不带状态，符合本次指定口径。见 [room-identity.ts:282](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:282>)、`:304`、`:323`。分房表、全岗总表、full 模板已消费；**整班机导出仍只写 `order.notes`**，未接伙伴备注及取消标注，见 [orders.export.ts:477](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export.ts:477>)。 |

# 二、修复回归及复审新发现

以下区分修复引入的问题与本轮新识别的遗漏。

## N1 · HIGH：迁移后仍可从 metadata 读出存量共享键

**类型：本轮新识别的泄露落点，迁移覆盖不足。**

旧编码房组被“按房组拆行”处理过时，`items[].metadata.splitRoomGroup.roomGroupId` 会存入完整的 `shared:<共享键>:<行 ID>`。

- 写入： [orders.service.ts:15668](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.service.ts:15668>)。
- 外部 metadata 脱敏仅过滤顶层已知键，未过滤 `splitRoomGroup`：`orders.service.ts:27556`；实际序列化接入为 `:27896`。
- 迁移只改 `Order.roomAssignment`：`rewrite-shared-room-group-ids.ts:128`。

**反例已复现：**房组 ID 改完后，metadata 中的 `shared:SHARED_SECRET:i-a` 仍完整透出。详情、列表及返回订单的售后响应均受影响。

**修复要求：**同步处理 metadata 历史副本，并以完整外部订单响应断言共享键值不存在。只测 `serializeRoomGroupsFor()` 不够。

## N2 · HIGH：拖空旧房迁入新房，乘客同时落普通组和共享组

**类型：B2 修复回归。**

将 S 全体乘客拖入 T，前端现在正确提交 `dissolve=[S]` 与新房 T；后端解散分支却把 S **全部成员**退回普通组，再追加 T，未排除本次迁走的人。

证据：[hotel-control.shared-rooms.ts:907](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/hotel-control.shared-rooms.ts:907>)、`:930`、`:967`。

**源码片段反例：**A 的同一乘客最终拥有普通组份额 `1` 和新共享组份额 `1`，行级汇总变成 `2`；B 也重复出现在两组。库存紧时可能拒绝本应成立的迁移，库存充足时则会保存错误状态。

**修复要求：**解散退普通组只承接本次未被其他目标房认领的成员。

## N3 · HIGH：隐式迁出只删除被搬成员，但删掉了全部留守方镜像

**类型：新增隐式旧房清理回归。**

S 有 A、B，只在请求中把 A 迁入 T，未显式提交 S：

- S 被纳入 touched，S 的所有旧 JSON 房组被整体丢弃：`hotel-control.shared-rooms.ts:875`。
- 数据库成员表只删除 A，B 留在 S：`:1090`。
- JSON 重建只覆盖显式 dissolve／rooms，没有重建隐式旧房留守成员。

**反例已复现：**B 的 `roomGroups=[]`，但 B 的成员记录仍在 S；随后 B 原归属行会回写 `roomsBilled=0`（`:1186`）。导出、提醒和房控开始读取不同状态。

**修复要求：**按隐式旧房剩余成员重建 JSON、份额和备注，并检查锁后乘客成员关系是否仍与发现阶段一致。

## N4 · MEDIUM：普通零份额只改备注，前端仍会保存成一间

**类型：零份额修复未覆盖前端。**

解绑／解散留下的普通 `roomFraction=0`，进入编辑器后被 `normalizeFraction(0,false)` 改为 `1`；保存普通组又只显式提交半间，整间省略。后端默认按 `1` 处理，无法进入新增的“原有普通零份额保留”分支。

证据：[RoomingEditor.tsx:196](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/admin-web/src/components/RoomingEditor.tsx:196>)、`:492`；`orders.routes.ts:1503、1522`。

**反例已复现：**普通零份额初始化结果为 `1`。后端接受零值不等于真实 UI 会提交零值。

## N5 · MEDIUM：只改共享房备注，也可能删除取消成员或被份额校验阻断

**类型：前后端修复语义冲突。**

前端把失效成员从保存集合删除（`SharedRoomWorkbench.tsx:516、537`）；后端则删除旧成员并按提交集合重建（`hotel-control.shared-rooms.ts:1126、1142`）。

两个直接反例：

- 活跃 A 份额 `1`，取消 B 份额 `0`：只改房备注即可删除 B 的历史成员及本单房组。
- 活跃 A 份额 `0`，取消 B 份额 `1`：只改备注时净份额变 `0`，前端 `:545` 拒绝保存；后端“未变更放行”完全用不上。

**修复要求：**灰显成员在无显式移除操作时原样回传；统计展示可区分活跃成员，但不能以展示过滤替代成员变更指令。

## N6 · MEDIUM：预编号丢掉入住日，跨日期导出房号发生回归

**类型：B10 修复引入。**

分房表与整班机先把 `byDate` 展平，预编号 scope 只含酒店；总表 scope 则含酒店和入住日。

证据：[orders.export-room-allocation.ts:673](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/orders.export-room-allocation.ts:673>)、`orders.export.ts:295`；`orders.export-master.ts:510`。

**反例已复现：**同酒店，16 日只有共享房 A，17 日只有共享房 B。分房表给 B **房2**，总表给 B **房1**；单独导出 17 日又回到房1。

**修复要求：**预编号、查询映射及未分房续号全部使用同一“酒店＋入住日”作用域。

## N7 · MEDIUM：拆单配对身份的排序仍依赖遍历顺序

**类型：确定性编号修复不完整。**

同一 `splitPairKey` 的两侧按各自 `orderNumber:groupId` 生成不同排序键；去重时保留首次遇见的键。

证据：[room-identity.ts:170](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:170>)、`:197`。

**反例已复现：**配对两侧单号 A、C，中间另有普通组 B。正序时配对房1、普通房2；倒序时普通房1、配对房2。

**修复要求：**同一身份必须具有唯一排序键，或聚合时选择稳定的最小键。

## N8 · MEDIUM：配对安全闸把同 token 的不同拆单关系合成一个集合

**类型：新增核验实现错误。**

`buildVerifiedSplitPairKeys()` 将相同 token 的所有 source／target 合并，再检查订单是否属于这个大集合，并未验证它们来自同一条拆单记录。

证据：[room-identity.ts:121](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/orders/room-identity.ts:121>)、`:140`。

**反例已复现：**A→B、C→D 使用同一 token，四单碰到相同本地组 ID；核验仍返回可信，三个生产导出会把两间无关房合成同号。

**修复要求：**按单条拆单关系核验；不同关系发生键冲突时不能合号。

## N9 · MEDIUM：存量重写脚本可能覆盖并发分房保存

**类型：新增迁移脚本风险。**

脚本先全量读取 JSON，之后按订单 ID 无条件写回整份旧快照，没有行锁、重读或版本条件。

证据：[rewrite-shared-room-group-ids.ts:84](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/scripts/rewrite-shared-room-group-ids.ts:84>)、`:128`。

运行期间若工作台已提交新成员，脚本可能覆盖其 JSON，而成员表保持新状态。**迁移应在锁内重读修改，或明确使用停止分房写入的维护窗口。**本次没有数据库证据证明迁移已执行。

### 锁定路径补充

正常、无重复成员的数据下，拖入／拖出、删除、份额和归属修改都有函数层锁定保护，见 `RoomingEditor.tsx:355、401、409、420`；未发现正常路径可成功绕过共享锁。

但 N2 产生重复乘客后，`:357` 只检查首个来源盒子，`:362` 却从所有盒子移除该乘客，可能在 UI 中改掉锁定组。单单后端仍会拒绝此请求，**这不是已证实的成功越权**，但会进一步造成编辑失败。

# 三、代理出口重新核验

| 出口 | 当前判断与证据 |
|---|---|
| **订单详情** | 按 requester 校验归属并角色化序列化，见 `orders.service.ts:5252、5258、27798`。新 UUID 房组本身不再编码共享键；**旧房组 ID／N1 metadata 仍是泄露口**。 |
| **订单列表** | 使用同一角色序列化器，见 `orders.service.ts:5113、5119`。与详情具有相同存量泄露面。 |
| **售后 warnings／订单响应** | 解绑代理文案只含本单份额及中性说明，不带伙伴单号、共享键或伙伴状态，见 [shared-room-unbind.ts:543](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/hotel-control/shared-room-unbind.ts:543>)。返回的 `order` 仍受 N1 影响。普通改期的嵌套 warnings 未展示，见 B5。 |
| **代理模板** | 服务端强制代理订单范围，见 `orders.routes.ts:872`；代理分支不查询伙伴 lookup，见 `orders.export-templates.ts:1381`；合住备注为中性文案，`:1035`。未发现新增共享伙伴信息输出。 |
| **全岗总表** | AGENT 角色强制为 `agent`，并传代理范围，见 `orders.routes.ts:943`；合住文案走中性分支，`orders.export-master.ts:772`。仍会内部查询 lookup，但未将伙伴信息写入代理表。**编号正确性不通过，见 N6–N8。** |
| **审计** | 审计查询端点要求 ADMIN／STAFF，见 [audit.routes.ts:12](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/audit/audit.routes.ts:12>)。但不能概括成“任何名为 audit 的响应都不对代理开放”：`reschedule-passengers` 会返回业务 `audit` 对象（`orders.routes.ts:3540`）；本次未发现其中新增共享键或伙伴单号。 |
| **提醒** | 提醒端点要求 ADMIN／STAFF，见 [reminders.routes.ts:34](</Users/bobwang/Documents/Flight Ticket Manager/.claude/worktrees/shared-rooms/backend/src/modules/reminders/reminders.routes.ts:34>)；外部订单序列化清空 reminders（`orders.service.ts:27832`）。状态机问题不构成代理泄露。 |
| **房控端点** | 共用 ADMIN／STAFF 鉴权（`hotel-control.routes.ts:78`），两个共享房端点均使用该配置（`:348、353`）。新增失效成员姓名／订单号仅在内部工作台返回。 |
| **批量改班次** | AGENT 被角色检查拒绝，见 `orders.routes.ts:1868`。正常成功的逐单 warnings 已返回；回包异常恢复分支仍丢警告。 |

**存量备注边界仍保留：**拆单会自动写入“由订单 … 拆分创建”（`orders.service.ts:18873`），订单 notes 和导出备注仍可对外呈现；自由备注也原文透传。这是既有问题，不能将“自动合住文案已脱敏”扩大成“整个响应与所有备注均不会出现关联信息”。
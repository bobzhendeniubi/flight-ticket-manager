# 功能合理性复审 · 双 AI 对审（2026-07-16）

> **内部审计资料，不进产品。** 基线 HEAD `92f7923`（feat/m2-agent-hierarchy-flights）。
>
> 方法：Claude（3 路代码核查 + 全部 comm doc 通读）与 codex（gpt-5.6-sol @ xhigh，独立通读 11 份文档 + 逐条核代码）各自独立审计 → 交叉辩论一轮（互相攻击证据、撤回误报）→ 本文为收敛后共识。上一轮 0715 审计全表见 `2026-07-15-需求原话重建-4月至7月.md`；已被 22ad62b/92f7923/f0d87e2/23b6c6e/c1496ee 修复的条目不再列。

---

## 一、本轮新发现（0715 没有的，双方共同确认）

| # | 问题 | 严重度 | 证据 | 一句话建议 |
|---|------|--------|------|-----------|
| N1 | **套餐多人单住：前后台报价公式分叉**。22ad62b 把「单住各占一间」算进后端 computeRoomsNeeded（soloRooms），前台镜像 bundleRooms.ts 仍写明「singleCount 不计入 roomsNeeded」→ 同一选择两个房数两个价 → checkout 带 expectedTotalCny 必被 PRICE_CHANGED 拒单，客户只看到「价格变化」流失。**已随 0716 部署在 staging 上活着。** | 高·立即 | sales-web/src/lib/bundleRooms.ts:7-15；backend orders.service.ts:5468-5500；CheckoutPage.tsx:504-507 | 删前台镜像公式改服务端 quote，至少同步口径；注意 22ad62b 自注「2成人1单住涨价需业务确认」尚未确认 |
| N2 | **切位「专卖不专」**。SeatAllocation 已落库有页面，但 flights search()/listSchedules() 与下单 CAS 都不减 ACTIVE 切位（orders.service 0 处引用）；getPoolAvailability 全仓无真实消费方 → 散客照样能买光切给代理的座，页面展示的「散客池」只在切位模块自己眼里存在。附带（中）：allocation.seats 不随代理销售递减，将来接入公共库存时会变真实少卖。 | 高 | seat-allocation.service.ts:1-16（自认不改共享口径）；orders.service.ts:655-672；seat-allocation.routes.ts:20-50 | 先关创建入口、标记现存切位不可依赖；补 consumed/remaining 并接入五条路径（搜索/锁位/下单/代理消费/回收）后再开放 |
| N3 | **/auth/register 后端裸开**。手册说账号由销售/客服开通、前端已藏注册页，但匿名 POST /auth/register 照样创建 CUSTOMER 并发 token——UI 伪装不是权限策略。 | 高 | auth.routes.ts:21-31；使用手册-前台.md:8；sales-web/src/App.tsx:158-160 | 删公开注册或改邀请制 |
| N4 | **常旅客 M1 越过拍板先上线**。提案标「待拍板」（含全号权限/导出审批），但 TravelerProfile 列表/详情/联想/CSV 已上线：STAFF 可见完整护照号、客户端直接导全号 CSV、导出无服务端审计。 | 高 | travelers.routes.ts:35-56；TravelerProfilesView.tsx:150-165,275-278；常旅客计划-proposal.md:3-4,86-92 | 冻结导出、列表默认脱敏，补岗位权限+服务端审计再放开 |
| N5 | **N/A 占位乘客污染旅客画像**。纯酒店/接送单前端伪造 documentNumber='N/A'（生日 1990-01-01）；画像按证件号聚合且不排除占位 → 不同客户的姓名/消费/同行关系聚成一个假人，还进录单联想。 | 高 | SingleOrderModal.tsx:1234-1241；traveler-profiles.aggregate.ts:116-118,215-244 | 聚合/联想/导出先排除 N/A；根治=不伪造 Passenger（isPlaceholder） |
| N6 | **积分提案把「存储字段」当成现成钱路径**。提案三次宣称复用 Order.discountTotal 即口径不变——该字段当前只存储和序列化，建单 total 不减它；照提案动工=积分扣了应付没减。 | 高（若动工） | 常旅客proposal:109-116；schema.prisma:1007-1010；orders.service.ts:619-625 | M2/M3 冻结，先定义折扣流水/总价公式/退款回冲/开票口径 |
| N7 | **业务文档同时描述三个互斥系统**。手册说切位是「刷新即失的演示」、系统逻辑全解说「schema 无模型」、代码已真实持久化；功能清单说 AI 是 offline 占位（实为匿名真 OpenAI）；更新日志说 191 上限可按班次调（字段已删）、档位线后台可调（硬编码无设置项）。业务方按哪本验收都在验另一个系统。 | 中 | 使用手册-后台.md:66-67；功能清单.md:15；更新日志.md:17,26；ticketing-cap.ts:1-9；flights.service.ts:64-67 | 指定唯一真源、文档绑定发布 commit，旧版标「已失效」 |
| N8 | **补房差无幂等 + 并发错账**（A15 追加）。roomSupplementBodySchema 无 idempotencyKey，每次调用无条件新增 FEE；读订单无 FOR UPDATE → 顺序重试=重复收，两并发=可能「两条 FEE、total 只含一条」。前端 submitting 挡不住 HTTP 重试/双标签页。 | 高 | orders.schemas.ts:826-843；orders.service.ts:5003-5014,4975-4988 | 幂等键+唯一约束；事务先锁单再从库重聚合 total |

## 二、0715 旧项 · 复核后仍站不住的（已修的不列）

**高：**
- **A16 改归属资金语义**：只改 agentId 不回溯资金，A 的多付按新 agentId 转成 B 的余额（仅 warning）；无 status/deletedAt 守卫；未 PAID 改归属 B 全额计佣。orders.service.ts:4872-4925,1925-1973
- **A1 佣金规则全仓只读**：写入口仅 seed；缺规则静默 0 佣金；AgentsPage 自认无入口。合同 6-01 验收范围内。orders.service.ts:6905-6929
- **A19 AI 匿名端点**（部分修正：全局 100/min 限流已有，「无限流」不再成立）：仍无认证无用户额度，匿名烧真模型；dateRank 与精确余位仍进模型上下文、靠提示词保密。ai.routes.ts:1-29；ai-assistant.ts:88-95,296-343
- **A2 汇率删除**：包机成本无原币/汇率/折算日，而 Q1 拍板的职责分工恰以「涉实付汇率」为前提——代码存不下拍板所依赖的证据。最小修=四元组 sourceCurrency+sourceAmount+fxRate+fxDate。schema.prisma:488-504
- **B12 已付/已出票/已完成单可直接改结算价**：disposal 闸不挡 PAID/TICKETED/COMPLETED、不查开票标记；改完不生成收退款/发票订正。orders.service.ts:2727-2805；funds-guard.ts:38-56
- **A17/A18 残余**：OTA 手填价 0–100000 无相对合理性防线（1000 打成 100/0 元静默过，仅留审计）。orders.schemas.ts:608-618

**中高：**
- **A12 代理永不过期占位**：AGENT 走后台录入通道 paymentExpiresAt=null，可绕开 9 张锁位上限占满整舱；无回收机制（账龄分桶只是可见）。orders.service.ts:225-238,516-520,818-825
- **B11（重定性）护照图 PII 爆炸半径**：base64 明文存业务表（单张≈3MB）、共库共备份共权限、后台详情整包携带、无访问审计/生命周期。20 张上限只是它的症状。（修正：客户/代理响应已剥图，「随任意订单下发」不成立）schema.prisma:1238-1239
- **A15 补房差房控不跟**（钱闸已补）：不碰 singleRoom/roomsBilled/分房，UI 却承诺「房控可见」。orders.service.ts:4942-5048
- **B14 签证金额非结构化**（修正版）：套餐签证金额取套餐**现行**挂牌价——套餐改价历史导出跟着变；任务无金额字段；备注自由文本已成下游数据通道。export-master.ts:252-270,457-475

**中：**
- **A11** 已售班次裸改时刻仅 WARNING，行程/酒店/签证/通知不联动。flights.service.ts:651-699,745-764
- **B10** roomsBilled 仍身兼计价+库存：拖拽改它不重算金额、多酒店塌首行、组内不校验性别/容量（超卖闸已接）。orders.routes.ts:917-1004
- **B9** 批量单联系人=录单员、电话='-'，下游航变找不到客人。orders.service.ts:2405-2449
- **B1** 已签证/全员自备签乘客的签证日期仍无可达录入口（HAS_VISA 不建任务→进不了唯一编辑 UI）。orders.service.ts:6645-6710
- **B5** 订单级 TICKETED 与航段三布尔仍两套真值（批量下拉只是藏了它）。schema.prisma:937-951
- **B7** 「回程未开」筛选仍捞进单程单（查询层无第二航段判断）。orders.service.ts:5185-5211
- **B23（升级）**「飞行次数」未拍板语义已被代码单方面定成「历史已出行次数」——建议列名先改成「历史已出行次数」再请业务确认。export-master.ts:205-212
- **A20（降级：高→中）**：ADMIN/STAFF only、role 只裁列不提权，非技术越权；真问题是七种岗位塌缩成 STAFF 的产品权限模型。若业务确认「所有 STAFF 可信」可关闭。
- **A5 残余**：概览 KPI 仍把缺成本跳过后报数字毛利，月度趋势却已改「未知」——同一页两种诚实度；BUNDLE 行仍不落成本快照。finances.service.ts:298-321
- **B20（重写）**：不是「预期到账≠total 违规」（Q9 拍板它就是独立人工账）而是**缺差额展示/原因/复核状态**——做非阻断警示，不做硬闸。
- **B15** 共用编码仍在备注自由文本（OTA 解析器复制进每人备注）。parseOtaRoster.ts:48-51,466-470

**低：** A3 「卖价低于它就亏」文案仍在（FinancesPage.tsx:873）；B24 DateRanking 僵尸 CRUD。

## 三、建议砍掉 / 冻结 / 降级（双方共识）

1. **砍** DateRanking 全套 CRUD（定价不消费、无后台页面，纯认知成本）
2. **并** 订单级 TICKETED → 由航段/履约派生（两套真值只制造对账争议）
3. **暂时下架** 切位创建入口（假成品比半成品危险——给运营错误安全感）
4. **砍** 公开 /auth/register（保留管理员建号/邀请制）
5. **删** backfill-finance-costs.ts（按售价 55%/65%/70% 伪造成本的 demo 脚本，FinancesPage 注释还在指路）
6. **降级** AI 助手 → 登录后按账号预算的 Beta；工具输出剥离 dateRank，别靠提示词保密
7. **冻结** 常旅客 M2/M3；M1 先补权限/审计/N-A 排除

## 四、立即行动 TOP3（按 真钱损失概率 × 发现难度）

1. **N1 套餐单住前后台分叉** —— 今晚已在 staging，直接丢成交且极难自查（客户只见「价格变化」）。同时补 22ad62b 遗留的「2成人1单住涨价」业务确认。
2. **N2 切位专卖不专** —— 运营一旦信这个页面，事故要到代理交名单才爆。先关入口。
3. **N5 N/A 占位乘客** —— 静默串档，发现时已污染联想/回填/隐私。先在聚合层排除。

## 五、辩论记录（谁撤了什么）

- codex 撤回「切位双重压缩真实库存」（getPoolAvailability 无真实消费方，双账并行是注释写明的设计），改判「专卖不专」高危 + 账模缺陷中危 —— Claude 异议成立。
- codex 将 A20 高→中（非技术越权，是产品权限模型失真）—— Claude 异议成立。
- Claude 撤回 B20 原表述（Q9 已拍板两账独立——又一次「先读拍板文档」的教训）、撤回 mockData 类型寄生入审计（type-only import，无运行时危害，转低优先级重构项）—— codex 反驳成立。
- codex 补漏承认：A2 上轮漏列是把「Q1-Q15 不重报」执行过宽——A2 恰是代码存不下 Q1 前提的证据，收录为高。

## 六、一句话结论

这轮和 0715 最大的不同：0715 抓的是「照做了但需求站不住」，这轮抓出来的主要是**「功能长得像成品，业务闭环没长」**——切位有表有页面没库存语义、常旅客有列表有导出没权限拍板、注册藏了前门开着后门、积分方案引用一个没人消费的字段。判据依旧是那句话：**这笔钱、这个座位、这间房，在另外三个岗位眼里变了没有？**

# Build outputs
# CDK
# Coverage
# Dependencies
# Docker
# Environment
# IDE
# Logs
# Misc
# OS
# Prisma
# Python
*.egg-info/
*.log
*.pptx~
*.py[cod]
*.swo
*.swp
*.tsbuildinfo
.DS_Store
.cache/
.docker/
.env
# C-19：挡住所有 .env 变体（含服务器上的 .env.prod / .env.staging，此前只挡了 .env 和 .env.*.local，
# 服务器 git add -A 会把生产密钥带进仓库）；两条 example 模板要继续可追踪，负向规则必须排在
# .env.* 之后（gitignore 按文件内顺序取最后一条匹配规则，放前面会被后面的 .env.* 覆盖回「忽略」）。
.env.*
!.env.example
!.env.*.example
.env.*.local
.env.local
.idea/
.next/
.nyc_output/
.pnp
.pnp.js
.turbo/
.venv/
.vscode/
Thumbs.db
__pycache__/
backend/prisma/migrations/dev/
build/
coverage/
dist/
infra/cdk.out/
node_modules/
npm-debug.log*
out/
pnpm-debug.log*
venv/
yarn-debug.log*
yarn-error.log*
~$*.pptx

# Office lock files
~$*.xlsx
~$*.docx
~$*.pptx

# agent worktrees + build caches
.claude/worktrees/
**/.swc/
.gstack/
